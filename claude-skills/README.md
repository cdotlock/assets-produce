# Claude Skills

Source packages for Claude Code skills that should be versioned with this
repository.

`novel-to-video/` uses the local `knowledge/novel-to-video/` pack and the
external `videoctl/bin/videoctl` CLI. It intentionally does not define an MCP
server or opencode built-in tool.
