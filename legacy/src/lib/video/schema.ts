/**
 * Video workflow — schema bootstrap.
 *
 * DEPRECATED: All tables now managed by Prisma schema.
 * This file kept for backward compatibility only.
 */

/**
 * No-op function — all tables now in Prisma schema.
 * Safe to call multiple times.
 */
export async function ensureVideoSchema(): Promise<void> {
  // All tables (novels, novel_scripts, domain_resources) are now
  // defined in prisma/schema.prisma and managed by Prisma migrations.
}
